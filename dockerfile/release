############################################################
# Dockerfile
############################################################
FROM microsoft/dotnet:latest

ENV ASPNETCORE_URLS http://+:6518

EXPOSE 6518

COPY /publish /publish

WORKDIR /publish

CMD ["dotnet", "webapp.dll"]
